import React from 'react';
import { Layout } from 'antd';

const Header = () => {
  return (
    <Layout>
      <Layout.Header className="header site-layout-background">Header</Layout.Header>
    </Layout>
  );
};

export default Header;
